# Disabled — this is a Next.js project, not a Deno project.
# Kept as a placeholder so git history isn't polluted with file deletions.
name: Deno

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deno workflow disabled for this Next.js project"
